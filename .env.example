# Port the service listens on. Render sets this automatically.
PORT=3000

# Log level: trace | debug | info | warn | error
LOG_LEVEL=info

# Optional: set this to require Authorization: Bearer <token> on all requests.
# Leave unset (or empty) to disable authentication entirely.
# API_KEY=your-secret-key-here
